# Optional: use this when you upgrade the deterministic analyst to a hosted LLM.
# OPENAI_API_KEY=sk-...

# Public app URL used when creating shareable links in production.
NEXT_PUBLIC_APP_URL=http://localhost:3000
